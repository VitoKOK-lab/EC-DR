# 跟 Claude 合作的規矩

## 部署：不要自動部署

**這個專案的 `main` 一被合併，GitHub Pages 就會自動上線給全公司用。
所以「合併」＝「部署」，兩件事是同一件事。**

規則：

- **不准自己合併 PR。** 一律停在草稿 PR，等我明確說「合併」才動。
- 我說「好了嗎」「看一下」「改好沒」都**不是**合併的意思，那只是問進度。
- 不確定我是不是要你合併的時候，就是不要合併，直接問我。

做完一輪工作應該停在這裡：推上分支 → 開草稿 PR → 跑完測試 → 回報，然後等我。

## 動正式資料庫要先講

Firestore 是正式資料，全公司即時同步。要直接改資料庫（不是改 code）之前：

- 先找出要改的那幾筆，**列出來給我看**，確認是對的再寫。
- 用 `updateMask` 只寫要改的欄位，其他一個都不要碰。
- 改完在 `logs` 留一筆操作紀錄，寫清楚是後台改的。
- 讀回來確認，把改動前後的值講給我聽。

## 改完 code 一定要跑過

- `node tests/run-all.js` 要全綠才算數。
- 每加一道防護，故意把它改壞一次，確認測試會變紅 —— 測不到的防護等於沒有。
- 改完一項就重跑一次全部，不要等最後才一起跑（修 A 弄壞 B 都是這樣漏掉的）。
