rules_version = '2';
// 內部小團隊用：只要通過匿名登入即可讀寫。
// 之後要更嚴格（例如限定 Email 網域、區分角色寫入權限）可再加條件。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
